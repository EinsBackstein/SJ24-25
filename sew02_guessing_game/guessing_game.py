# import the function 'randint' from the standard module 'random'
from random import randint

# the maximum number (should be asked from the user)
max_number = 10

# the function randint generates a random number from 1 to max_num
random_number = randint(1, max_number)
